//go:build !cgo || ignore_gui_tests
// +build !cgo ignore_gui_tests

package ui

// Test file excluded from normal test runs
// GUI tests require CGO and are not needed for core functionality testing
